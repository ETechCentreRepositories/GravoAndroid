<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.greenravolution.gravodriver">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_gravo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_gravo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".loginsignup.Login" />
        <activity android:name=".loginsignup.LoginActivity" />
        <activity android:name=".loginsignup.RegisterActivity"></activity>
    </application>

</manifest>